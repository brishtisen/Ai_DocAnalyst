{
  "name": "ai-pdf-chatbot-root",
  "version": "1.0.0",
  "description": "Root workspace for AI PDF Chatbot",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "build:all": "npm run build --prefix frontend",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "react-markdown": "^10.1.0",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  }
}
